@page
@model EventPlannerWebApplication.Pages.MyEvents.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MyEvent[0].Mentiune)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MyEvent[0].TipEveniment.DenumireEveniment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MyEvent[0].Locatie.DenumireLocatie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.MyEvent) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Mentiune)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TipEveniment.DenumireEveniment)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Locatie.DenumireLocatie)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
